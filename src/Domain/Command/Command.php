<?php declare(strict_types=1);

namespace Mediagone\CQRS\Bus\Domain\Command;


interface Command
{
    // The Command interface implements no method definition because it is just a PHP object that hold data.
    // However, using an interface makes the code more readable, type safe and open to extension.
}
